# Same OpenBao secret already seeded for cert-manager's ClusterIssuer,
# materialized into this namespace too.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cloudflare-dns-token
  namespace: external-dns
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: cloudflare-dns-token
    creationPolicy: Owner
  data:
    - secretKey: api_token
      remoteRef:
        key: cloudflare-dns-token
        property: api_token
